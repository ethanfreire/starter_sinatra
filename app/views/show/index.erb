<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>List of shows</title>
  </head>
  <body>
    <ul>
    <%@shows.each do |show|%>
      <h2 id = "<%=show.id%>"> </h2>
    <a href=<%="/shows/#{show.id}"%>>
      <h3><%=show.name%> , <%=show.city%></h3> </a></br>
      <%end%>

      </ul>


  </body>
</html>
